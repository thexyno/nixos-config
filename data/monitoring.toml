[master]
hostname = "beliskner"
ip = "beliskner.kangaroo-galaxy.ts.net" # tailscale

[hostOverrides]

[exporters.nginx]
hosts = [
  "ds9",
  "picard"
]

[exporters.node]
hosts = [ "ds9", "picard" ]

[exporters.smartctl]
hosts = [ "ds9" ]

[exporters.blackbox]
hosts = [ "beliskner" ]

[promtail]
hosts = [ "picard", "ds9" ]
